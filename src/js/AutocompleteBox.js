"use strict";

function AutocompleteBox(input, querySource) {


  // handle change event
  // throttle / delay and wait some time before send request
  // send request
  // print result of the request in drop down box
  // trigger search if (TAB or enter or "search icon" ) clicked
  // update view

  function init() {

  }

  this.open = function(){};
  this.close = function(){};
  this.next = function(){};
  this.prev = function() {};
  this.select = function(){};
  this.eval = function(){};

}
